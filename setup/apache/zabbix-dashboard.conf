# Define /zabbix-dash alias, this is for the
<IfModule mod_alias.c>
    Alias /dashboard /usr/share/zabbix-dash2
</IfModule>

<Directory "/usr/share/zabbix-dash2">
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
